# SYT-036: Home Hover Stale Card Lifecycle Regression

## Status

- Task ID: `SYT-036`
- GitHub issue: #36
- Branch: `swarm/syt-036-home-hover-stuck-lifecycle`
- PR: pending
- State: Needs Review after push/PR
- Role: Controller/Runner

## Scope

Fix the live Home native hover regression reported after `SYT-031`: after a refresh, watch-page navigation, and SPA return to Home, multiple Home cards can retain YouTube hover backgrounds and native hover preview autoplay can fail.

## Non-Scope

- Do not reintroduce enhanced Home/Search hover grow, highlight, or #8 behavior.
- Do not touch version, release, tag, Web Store assets, popup settings, or unrelated watch-page behavior.
- Do not close #10; this is a separate bug lane.

## Parallelization

- `parallel-safe`: no
- `serial-required`: yes
- `depends-on`: `SYT-031` and `SYT-010G` integrated
- `conflict-risk`: high, live YouTube SPA/native preview lifecycle

## Files Touched

- `src/content/grid-hover.ts`
- `tests/e2e/youtube-fixtures.ts`
- `tests/e2e/extension.fixture.spec.ts`
- `docs/swarm/handoffs/SYT-036.md`
- hot swarm docs for current routing state

## Changes

- Track the feed card/target that native hover recovery synthetically entered.
- Dispatch paired synthetic `pointerout`/`mouseout`/`pointerleave`/`mouseleave` before nudging a different card or clearing navigation/pointer state.
- Keep synthetic recovery events from rewriting the cached physical pointer coordinates used for card geometry lookup.
- Add a Home fixture regression with a stale full-page preview overlay over two Home cards; the test asserts only the current card remains in the simulated YouTube hover state.
- Keep Home/Search feed hover native-only; no grow/highlight classes are reintroduced.

## Verification

- `npm run test:e2e -- --grep "home fixture clears stale synthetic native hover"`: PASS.
- `npm run test:e2e`: PASS, 13 fixture tests.
- `npm run validate:all`: PASS, including typecheck, lint, package validation, 22 unit tests, and 13 fixture tests.
- `npm run test:e2e:live`: PASS, 2 live smoke tests including watch-to-Home native hover preview.

## Risks

- Medium/high. This patch touches the fragile recovery path for YouTube native Home/Search preview behavior. Fixture coverage now catches the stuck-card pattern, but live YouTube SPA experiments can still vary.

## Next Action

Push the branch, open a draft PR linked to #36, then review before integration. Because this is live YouTube hover behavior, prefer user/live QA before merge unless the controller explicitly records a waiver with evidence.
